{
  "name": "piano-log",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:data": "tsx scripts/generate-practice-json.ts",
    "generate:readme": "tsx scripts/generate-readme-grid.ts",
    "generate": "npm run generate:data && npm run generate:readme"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "typescript": "^5.8.3",
    "tsx": "^4.20.3"
  }
}
