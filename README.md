# Piano Log

This repository now contains the Piano Log application.

The previous Markdown-based practice-log workflow and its GitHub Actions bot are preserved in [archive/markdown-workflow](archive/markdown-workflow/README.md). They are inactive: no workflow remains under `.github/workflows`, so pushes no longer trigger generated JSON updates or bot commits.

## Run the app

```bash
cd client
npm install
npm run dev
```
